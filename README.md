# Sugarscape Modified

This project extends the Sugarscape model by introducing a local mutual-aid mechanism, where agents share resources with vulnerable neighbors.  

The model examines how local sharing affects inequality, survival, and resource distribution.  

All modifications are explicitly marked with comments in the code for clarity and reproducibility.
